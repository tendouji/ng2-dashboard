import { CommaSeparatorPipe } from './comma-separator.pipe';

describe('CommaSeparatorPipe', () => {
  it('create an instance', () => {
    const pipe = new CommaSeparatorPipe();
    expect(pipe).toBeTruthy();
  });
});
