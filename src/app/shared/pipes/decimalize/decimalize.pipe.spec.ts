import { DecimalizePipe } from './decimalize.pipe';

describe('DecimalizePipe', () => {
  it('create an instance', () => {
    const pipe = new DecimalizePipe();
    expect(pipe).toBeTruthy();
  });
});
