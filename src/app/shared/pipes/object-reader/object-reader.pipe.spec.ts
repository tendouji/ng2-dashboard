import { ObjectReaderPipe } from './object-reader.pipe';

describe('ObjectReaderPipe', () => {
  it('create an instance', () => {
    const pipe = new ObjectReaderPipe();
    expect(pipe).toBeTruthy();
  });
});
